---
// Process Section
---

<section class="process-section relative w-full py-24 overflow-hidden" style="z-index: 1;">
  <div class="max-w-6xl mx-auto px-6 sm:px-8 lg:px-10">
    <!-- Section Header -->
    <div class="text-center mb-16">
      <span class="text-sm uppercase tracking-[0.45em] text-cyan-400/70">How we work</span>
      <h2 class="font-biennale text-white mt-4" style="font-size: 48px; line-height: 1.1; font-weight: 500; letter-spacing: -0.02em;">
        Simple. Strategic.<br>
        <span class="gradient-text">Effective.</span>
      </h2>
    </div>

    <!-- Process Steps -->
    <div class="grid md:grid-cols-2 lg:grid-cols-4 gap-6">
      {[
        {
          number: '01',
          title: 'Discovery Call',
          description: 'We talk about your goals and what your business actually needs.',
          color: 'purple'
        },
        {
          number: '02',
          title: 'Plan & Design',
          description: 'Clear strategy, visuals, and structure mapped to your goals.',
          color: 'cyan'
        },
        {
          number: '03',
          title: 'Build & Launch',
          description: 'SEO-friendly, mobile-ready, and fast to load.',
          color: 'purple'
        },
        {
          number: '04',
          title: 'Support',
          description: 'Hosting, updates, or ongoing marketing when you\'re ready to grow.',
          color: 'cyan'
        }
      ].map((step) => (
        <div class="process-card group">
          <div class={`gradient-line-left gradient-${step.color}`}></div>
          <div class="card-content-process">
            <div class={`step-number gradient-text-${step.color}`}>{step.number}</div>
            <h3 class="text-xl font-bold text-white mb-3">{step.title}</h3>
            <p class="text-gray-300 leading-relaxed text-sm">{step.description}</p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  /* Process Cards */
  .process-card {
    position: relative;
    background: rgba(10, 10, 15, 0.85);
    border-radius: 16px;
    overflow: hidden;
    transition: all 0.3s ease;
    backdrop-filter: blur(12px);
    height: 100%;
  }

  .process-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
  }

  /* Gradient Left Line */
  .gradient-line-left {
    width: 3px;
    height: 100%;
    position: absolute;
    left: 0;
    top: 0;
  }

  .gradient-purple {
    background: linear-gradient(180deg, #b24bf3 0%, #8b5cf6 100%);
    box-shadow: 0 0 20px rgba(178, 75, 243, 0.5);
  }

  .gradient-cyan {
    background: linear-gradient(180deg, #06b6d4 0%, #00d9ff 100%);
    box-shadow: 0 0 20px rgba(0, 217, 255, 0.5);
  }

  .card-content-process {
    padding: 2rem 1.5rem;
  }

  /* Step Numbers with Gradient */
  .step-number {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    opacity: 0.4;
  }

  .gradient-text-purple {
    background: linear-gradient(135deg, #b24bf3 0%, #8b5cf6 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .gradient-text-cyan {
    background: linear-gradient(135deg, #06b6d4 0%, #00d9ff 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .step-number {
      font-size: 2.5rem;
    }
  }
</style>

